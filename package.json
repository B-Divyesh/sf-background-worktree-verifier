{
  "name": "background-worktree-verifier-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite --config site/vite.config.js",
    "build:site": "vite build --config site/vite.config.js",
    "build": "npm run build:site",
    "test": "cargo test && npm run build:site && node --test site/test/site.test.mjs",
    "preview": "vite preview --config site/vite.config.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.0",
    "playwright": "1.58.2",
    "vite": "^6.1.0"
  }
}
